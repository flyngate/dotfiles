
* add `bash` package
* zsh, bash
  - macports autodiscover
  - ctrl+left and fn+left for macos
  - automatically load scripts from .zsh.local directory
  - check for path presense before PATH modifying
  - handle detached HEAD in prompt
* scripts
  - text `extract` script
  - `serve` script
